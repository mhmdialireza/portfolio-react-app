import { Landing, Navbar, TopBar } from './../../components'

const Header = () => {
    return (
        <header class="header">
            <TopBar />
            <Navbar />
            <Landing />
        </header>
    )
}

export default Header